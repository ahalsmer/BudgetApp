<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model ="clr-namespace:BudgetApp.Models"
             x:Class="BudgetApp.MainPage">

    <StackLayout>
        <Button x:Name="goal" Text="Set Your Goal" Clicked="Add_Goals" />
        <ListView x:Name="ExpenseView"
          Margin="20"
          ItemSelected="ExpenseView_ItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:Expense">
                <ViewCell>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Image Grid.Row="0" Source="/Assets/Images/money-in-hand-icon.png" />
                            <Label Grid.Row="0" Text="{Binding Type}" VerticalOptions="CenterAndExpand" FontSize="16" TextColor="Black" />
                            <ProgressBar Grid.Row="1" Progress="{Binding AmountProgress}" ProgressColor="Green" />
                            <ProgressBar Grid.Row="1" Progress="{Binding GoalProgress}" ProgressColor="Red" />
                        </Grid>
                    </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    </StackLayout>

</ContentPage>
